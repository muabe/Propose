package com.muabe.propose.remind;

public class Layer {
    private Node root;

    public Layer(Node root){
        setRoot(root);
    }

    public void setRoot(Node root){
        this.root = root;
    }
}
